package utils

import (
	"testing"
)

func TestCartesianProduct(t *testing.T) {
	a := []string{"a", "b"}
	var b []string
	b = a

	t.Log(Product2(a, b, 1))
	//nt  := Alphabet + Number
	//for i :=range nt{
	//	fmt.Println(string(nt[i]))
	//}
}
